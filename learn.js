// Создать переменную price и присвоить ей значение 28.99
var price=28.99;
// Создать переменную discount и присвоить ей значение 10
var discount=10;
// Вычислить цену со скидкой и присвоить переменную total.
var total=price - (price*(discount/100));
// Сравнить переменную total и 25, если переменная больше 25, тогда выполнить фрагмент кода freeshiping
if (total > 25) {
	freeShipping();
// Конец команды if
}
// Создаём переменную count и присваиваем ей значение 10
var count=10;
// Пока перемення count больше нуля, выполняем фрагмент кода juggle и уменьшаем значение переменной count на 1
while (count>0) {
	juggle ();
	count=count-1;
// Конец цикла while
}
// Создаем переменную dog с атрибутами: name:Rover, weight:35. Если переменная dog весит больше 30 кг вывести сообщение вуф,вуф. Закрываем команду if.
var dog="name="Rover, weight="35";
if (dog.weight>30) {
	alert ("WOOF WOOF");
} else {
	alert ("woof woof");
}
var circleRadius=20;
var ciroleArea =
	Math.PI * (circleRadius * circleRadius);

